{
  "name": "mockingbird-backend",
  "version": "1.0.0",
  "description": "Sarcasm Translator Backend API",
  "main": "src/server.ts",
  "type": "module",
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun --watch run src/server.ts"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
